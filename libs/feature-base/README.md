# @reddoc/feature-base

Componentes base configuracionales del ERP (Fase 4+ del framework).

Consumen `EntityConfig` resuelto por los resolvers de `@reddoc/core/module-config`
y delegan I/O al `EntityDataGateway` inyectado.

Ver `docs/architecture/erp-module-architecture.md`.
